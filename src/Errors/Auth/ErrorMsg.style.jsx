import styled from "styled-components";
import { ErrorMessage } from "formik";

export const ErrorMsg = styled.div`
  border-color: #fc8181;
  color: #fc8181;
  font-size: 1rem;
  margin-top: 0.25rem;
  bottom: -10px;
  align-self: center;
`;
